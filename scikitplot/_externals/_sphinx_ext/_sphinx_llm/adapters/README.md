# `adapters` ownership placeholder

Semantic adapters for custom Sphinx/docutils nodes, media, tabs, grids, dropdowns, plots, galleries, JupyterLite, YouTube, and raw-content policy.

Production implementation is intentionally deferred to the numbered maintenance checkpoints.
