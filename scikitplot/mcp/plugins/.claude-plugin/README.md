# scikit-plots docs — Claude Code plugin

Read-only documentation search for scikit-plots over MCP.

```bash
claude mcp add scikitplot-docs python -m scikitplot.mcp
```

Or add `.mcp.json` to your Claude config. The `scikitplot mcp` console command is equivalent to `python -m scikitplot.mcp`.
