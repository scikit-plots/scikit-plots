# scikitplot.mlflow test suite

## Install test deps

```bash
python -m pip install -U pytest
# YAML tests are optional (skipped if not installed)
python -m pip install -U pyyaml
```

## Run

```bash
pytest
```
