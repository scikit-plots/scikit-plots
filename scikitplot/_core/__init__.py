# from scikitplot._core.__cpu__ import (
#     __cpu_features__,  # Runtime truth — what the actual CPU supports {'MMX': True, 'SSE': True, 'SSE2': True, ..., 'AVX512F': False, ...}
#     __cpu_baseline__,  # Build-time minimum — what every machine running this build must have ['X86_V2']
#     __cpu_dispatch__,  # Build-time dispatch targets — what the library was compiled to optionally use ['X86_V3', 'X86_V4', 'AVX512_ICL', 'AVX512_SPR']
# )
# from . import __cpu__
