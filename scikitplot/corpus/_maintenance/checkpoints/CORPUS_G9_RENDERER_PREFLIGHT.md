# CORPUS G9 — Gallery Renderer Preflight

Date: 2026-08-19
Status: **VERIFIED RENDERER PREFLIGHT / FULL SPHINX ENVIRONMENT BLOCKED**

## Source authority

```text
archive  scikit-plots(20260818-204251).zip
sha256   4f62b638647a1645ad6174b646c2aabb4b30bee4a28ac9afb12d25ee71ff4b27
```

Gallery input: the seven-file canonical G8 collection.

## Goal

Verify the Sphinx-Gallery-facing structure of the canonical Corpus examples
without weakening the earlier execution/capability gates.

A true project Sphinx build remains a separate gate because the uploaded source
archive does not contain the repository `docs/` tree and the current execution
environment cannot install the declared Sphinx dependencies due DNS/network
resolution failure.

## Finding G9-01 — sub-gallery README class-role context

The G8 `README.txt` used short roles such as:

```rst
:class:`FluentCorpus`
:class:`RuntimeCorpus`
:class:`VectorIndexBackend`
```

but did not set a current module. Individual example docstrings already set
`.. currentmodule:: scikitplot.corpus`.

G9 adds the same directive to the Corpus gallery README:

```rst
Corpus examples
===============

.. currentmodule:: scikitplot.corpus
```

This keeps short public API cross-references unambiguous when Sphinx renders the
sub-gallery index.

## Renderer-adjacent static gate

Checked the canonical collection for:

```text
README/header reST title                      PASS
README currentmodule                          PASS
7/7 module header docstrings                  PASS
7/7 module reST titles/underlines             PASS
7/7 currentmodule directives                  PASS
7/7 gallery tags blocks                       PASS
canonical # %% block splitters                PASS
commented code-block indentation              PASS
public :class: role resolution                32/32 PASS
static preflight errors                       0
```

Sphinx-Gallery's documented script syntax requires a reST-titled module
header and supports `# %%` blocks for embedded reST. The gallery subsection
header is supplied through `README.txt`, the backward-compatible header name.

## Execution regression gate

After the README fix:

```text
compileall                                     PASS
portable collection                           7/7 PASS
network opt-in                                disabled
Whisper/ASR opt-in                            disabled
```

No gallery Python file changed in G9.

## Full renderer attempt

The applied `pyproject.toml` declares the documentation dependencies including
`sphinx[testing]` and `sphinx-gallery`, but they are not installed in this
harness.

An isolated installation attempt failed before package resolution because this
runtime cannot resolve the package index host. Therefore:

```text
real sphinx-build                              BLOCKED / environment
real Sphinx-Gallery page generation            BLOCKED / environment
project docs/conf.py verification              BLOCKED / docs tree absent from upload
```

These are not marked PASS.

## Public integration evidence

The published development documentation exposes an `auto_examples/corpus`
sub-gallery and identifies the pages as generated by Sphinx-Gallery. This
supports the integration target, but does not replace the missing local project
renderer gate.

## Corpus implementation impact

None.

G9 changes only the gallery subsection README plus maintenance metadata.

## Next exact action

In a complete project checkout that includes `docs/` and the project's declared
documentation environment:

1. apply the canonical G9 Corpus gallery collection;
2. run the repository's normal Sphinx/Sphinx-Gallery HTML target;
3. treat warnings/errors as failures;
4. inspect the generated Corpus index, example ordering, class links, tags,
   downloads, thumbnails, and captured outputs;
5. if green, close the Corpus gallery UX campaign and return to normal
   maintenance/cross-module work.
