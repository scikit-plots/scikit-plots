# .binder/runtime.txt
#
# Authors: The scikit-plots developers
# SPDX-License-Identifier: BSD-3-Clause
#
# Specifies the Python (or R) runtime for repo2docker.
# Format: <language>-<version>  (e.g. python-3.13, r-4.3.2)
# Blank lines and lines starting with # are ignored by repo2docker.
#
# Note: when .binder/Dockerfile is present, repo2docker uses it directly
# and runtime.txt is ignored.  Keep the version here in sync with the
# Python version installed in the Dockerfile so that the two build paths
# remain equivalent.
#
# R runtime (disabled; uncomment — and remove the python line — to switch):
# r-<YYYY>-<MM>-<DD>
# r-4.3.2-2024-01-10
python-3.13
