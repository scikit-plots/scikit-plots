# `_sphinx_ai_assistant` machine schemas

Schemas cover maintenance state plus planned client/server configuration contracts. `discovery-contract.schema.json` is intentionally non-secret; `setting-definition.schema.json` provides the fields needed to make sensitivity and persistence mechanically reviewable during B10.
