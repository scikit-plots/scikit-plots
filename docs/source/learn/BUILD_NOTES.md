# learn/terminology — build notes

Generated by `build_terminology.py` (deterministic, idempotent).

## What's here
- `terminology/index.rst` — semantic grid hub (26 theme cards, 3 levels) + hidden toctree
- `terminology/NNN-<slug>.rst` — one self-contained page per term (001–431, stable id = inventory order)
- `data_preparation_and_analysis/`, `bayesian_data_analysis/`, `time_series/`, `deep_learning/` — topic hub pages
- `index.rst` — parent learn index (toctree of the above)

## Source-of-truth files (edit these, then regenerate)
- `term_inventory.tsv` — 431 verified `title <TAB> url` rows (frozen)
- `build_terminology.py` — themes, glosses, page + grid emission
- `term_content.py` — `CONTENT` (full per-term bodies) and `MINDMAP` (cross-links); grows batch by batch

## Regenerate
```bash
python build_terminology.py   # clears old NNN-*.rst, rewrites index + 431 pages
```
Fail-fast: aborts if inventory ↔ CONTENT/MINDMAP keys mismatch.

## Full-content progress
75 / 431 pages have full source-faithful bodies (rest render a working summary until written).
